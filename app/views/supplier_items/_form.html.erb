<%= form_with(model: supplier_item, local: true) do |form| %>
  <% if supplier_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(supplier_item.errors.count, "error") %> prohibited this supplier_item from being saved:</h2>

      <ul>
      <% supplier_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :supplier_id %>
    <%= form.text_field :supplier_id %>
  </div>

  <div class="field">
    <%= form.label :item_id %>
    <%= form.text_field :item_id %>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.number_field :quantity %>
  </div>

  <div class="field">
    <%= form.label :cost %>
    <%= form.text_field :cost %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
