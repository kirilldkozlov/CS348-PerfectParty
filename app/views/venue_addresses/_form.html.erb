<%= form_with(model: venue_address, local: true) do |form| %>
  <% if venue_address.errors.any? %>
    <div class = "error-title">
      <h2><%= pluralize(venue_address.errors.count, "error") %> prohibited this venue_address from being saved:</h2>
    </div>

      <ul>
      <% venue_address.errors.full_messages.each do |message| %>
        <div class = "error-message">
          <li><%= message %></li>
        </div>
      <% end %>
      </ul>
  <% end %>

  <div class="field">
    <%= form.label :venue_id %>
    <%= form.text_field :venue_id %>
  </div>

  <div class="field">
    <%= form.label :address_id %>
    <%= form.text_field :address_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
