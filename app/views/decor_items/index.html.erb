<p id="notice"><%= notice %></p>

<h1>Decor Items</h1>

<table>
  <thead>
    <tr>
      <th>Id</th>
      <th>Decor</th>
      <th>Item</th>
      <th>Quantity</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @decor_items.each do |decor_item| %>
      <tr>
        <td><%= decor_item.id %></td>
        <td><%= decor_item.decor %></td>
        <td><%= decor_item.item %></td>
        <td><%= decor_item.quantity %></td>
        <td><%= link_to 'Show', decor_item %></td>
        <td><%= link_to 'Edit', edit_decor_item_path(decor_item) %></td>
        <td><%= link_to 'Destroy', decor_item, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Decor Item', new_decor_item_path %>
