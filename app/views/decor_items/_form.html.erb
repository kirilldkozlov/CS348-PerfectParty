<%= form_with(model: decor_item, local: true) do |form| %>
  <% if decor_item.errors.any? %>
    <div class="error-title">
      <h2><%= pluralize(decor_item.errors.count, "error") %> prohibited this decor_item from being saved:</h2>
    </div>
      <ul>
      <% decor_item.errors.full_messages.each do |message| %>
        <div class="error-message">
          <li><%= message %></li>
        </div>
      <% end %>
      </ul>
  <% end %>

  <div class="field">
    <%= form.label :decor_id %>
    <%= form.text_field :decor_id %>
  </div>

  <div class="field">
    <%= form.label :item_id %>
    <%= form.text_field :item_id %>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.number_field :quantity %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
